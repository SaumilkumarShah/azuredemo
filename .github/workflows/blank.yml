# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - name: Set Variable
        uses: deep-mm/set-variables@v1.0
        with:
            # Name of variable file
            variableFileName: 'dev' #Dont write .json here

      # Read Variable from dev.json
      - name: Run a one-line script
        run: echo Hello, ${{ env.variable1 }}

      - name: Read template from file
        id: template
        uses: traversals-analytics-and-intelligence/file-reader-action@master
        with:
          file: .github/workflows/b11.yml

      - name: Get the template output
        run: echo "${{ steps.template.outputs.content }}"
      
